# Optic Shield Frontend Environment Configuration
# Vite uses VITE_ prefix for client-side environment variables

# Primary API endpoint (ngrok Cloud GPU backend)
VITE_API_PRIMARY_URL=https://trailing-matriarch-marshland.ngrok-free.dev

# Backup API endpoint (Modal Serverless GPU)
VITE_API_BACKUP_URL=https://mgtd2104--optic-shield-backend-health.modal.run

# Request timeout in milliseconds
VITE_API_TIMEOUT=8000